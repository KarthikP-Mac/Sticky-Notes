Packages used in this project:

Dependencies:

react — React UI library
react-dom — React DOM renderer
lucide-react — icon components
crypto-js — encryption/decryption utilities
firebase — Firebase SDK
jwt-decode — decode JWT tokens


Dev dependencies:

vite — build tool / dev server
@vitejs/plugin-react — Vite React support
eslint — linting
@eslint/js — ESLint core rules
eslint-plugin-react-hooks — hook lint rules
eslint-plugin-react-refresh — React refresh lint support
@types/react — TypeScript types for React
@types/react-dom — TypeScript types for React DOM
globals — global variable definitions for ESLint